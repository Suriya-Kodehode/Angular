# ─── Docker Compose ───────────────────────────────────────────────────────────
# Used by docker-compose.yml for variable substitution only.
# Must match the password in MONGODB_URI inside projects/server/.env.
MONGO_ROOT_PASSWORD=