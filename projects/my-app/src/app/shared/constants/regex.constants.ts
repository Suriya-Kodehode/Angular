// Shared regex constants used by the app for API routing and proxy handling.
// Keep stable, reusable regex values centralized here.
export const API_PREFIX = '/api';
export const API_PREFIX_REGEX = /^\/api/;
